{
  "profile": "default",
  "region" : "ap-northeast-1",
  "configuration": "Release",
  "framework": "netcoreapp2.0",
  "configuration": "Release",
  "stack-name": "update-dns",
  "s3-bucket": "my-bucket",
  "s3-prefix": "updateDns/",
  "template": "serverless.template",
  "template-parameters": "ExecutionRole=<role-arn>;HostZoneId=<zone-id>;FqdnWhitelist=<fqdn-csv>"
}
